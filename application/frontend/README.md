# OIO customer website

Next.js 15 App Router. Phase 3.

```bash
cp .env.example .env.local
npm install
npm run dev
```

API: `NEXT_PUBLIC_API_URL` (default `http://localhost:8080`). Run FastAPI on 8080.

`npm run lint` · `npm run typecheck` · `npm test` · `npm run build`
